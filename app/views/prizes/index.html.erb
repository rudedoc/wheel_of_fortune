<%- model_class = Prize -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
  <p>The system must have exactly 12 Prize records in order for the wheel to work properly, at somestage it will be upgraded to work with a variable number of prize records, until then the ability to delete or create records has been disabled, you can only edit existing entries.</p>
</div>
<table class="table">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @prizes.each_with_index do |prize, index| %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= link_to prize.title, edit_prize_path(prize) %></td>
        <td><%= prize.description %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_prize_path(prize), :class => 'btn btn-mini' %>
          <%# link_to t('.destroy', :default => t("helpers.links.destroy")),
                      prize_path(prize),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_prize_path,
            :class => 'btn btn-primary' %>
